# Copy to .env and fill in only if you use the optional MOCK_LLM=0 extension.
# Left as-is (or with MOCK_LLM=1 / unset), the service runs fully offline.
MOCK_LLM=1

# Only needed when MOCK_LLM=0 — a free-tier Groq API key (console.groq.com).
GROQ_API_KEY=